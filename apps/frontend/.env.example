VITE_SUPABASE_URL=
VITE_SUPABASE_KEY=
VITE_SB_PUBLISHABLE_KEY=

# Express Backend API
VITE_API_URL=http://localhost:3000/api/v1

# Optional: dev override for company ID (remove once auth flow is complete)
VITE_DEV_COMPANY_ID=

# ==============================================================
# SERVER-ONLY secrets — NEVER prefix with VITE_ (would leak into
# the client bundle). Used by local seed scripts / CLI tools only.
# ==============================================================
SERVICE_ROLE_KEY=
SB_SECRET_KEY=
